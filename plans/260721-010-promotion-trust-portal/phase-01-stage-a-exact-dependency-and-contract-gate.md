---
phase: 1
title: "Stage A exact dependency and callable scaffold gate"
status: pending
priority: P1
dependencies: []
effort: "M"
---

# Phase 1: Stage A exact dependency and callable scaffold gate

## Overview

Create the fresh v3 recovery worktree from exact integration only after independent validation and
readiness. Re-prove the 85 released inputs and create one exact 22-path semantics-free callable
scaffold commit. No test or target lesson semantic exists in this phase.

## Context Links

- [V3 recovery chronology](./stage-a-release-amendment.md#scaffold-first-commit-chronology)
- [Release gates](./dependency-and-release-gates.md#gate-a--static-portal-authority)
- [Architecture](./architecture-and-api-boundaries.md#decision)
- [Complete test contract](./verification-evidence-and-uat.md#complete-tests-only-portfolio)

## Requirements

### Recovery gate

- Fresh validation and later readiness outputs must authorize the recovery; this correction alone
  does not.
- Create `feature/issue-10-portal-stage-a-v3` in a new worktree from exact
  `5644f01b4c0443a81f3af0bcce80f44c847cd986`.
- Before the scaffold, seed exact correction subtree
  `92b620712d6fdbf69eb0901554dcf27ae357bc7e` (18 Markdown files), apply the exact
  `a9e5ee2…df013a3` validation delta to reach subtree
  `60e9f41b517cf7ecfda9f85ce5038f8fadd8d981`, then apply only the exact
  readiness delta from `df013a3…` to the 40-hex output named in the Issue #10 handoff. Prove the
  final subtree equals the published readiness output and every seed/delta is plan-directory-only.
- Preserve failed PR #29/branch/commits/evidence and all stopped worktrees. Cherry-pick/copy no v2
  product, test, evidence, log, trace, manifest, fixture, or generated byte.
- Re-prove release ancestry/tree, 85 blobs/bytes/SHA-256, seven protected identities, exact root
  Make seam/nine reservations, exact Node/npm/Python/package locks, and 33/33 product-path absence.

### Scaffold gate

- The adapter, provider, catalog, router, static and React render, static-route generator, built
  server, lifecycle, evidence writer, and all nine public Make delegates are callable.
- The build serves one bounded neutral document with an empty unadmitted descriptor set.
- The scaffold may parse/dispatch/serve generic state only. No target lesson/step/fixture/outcome,
  13-route list, expected-value branch, duplicate default truth, forced failure, mock, skip, or
  final semantic copy is allowed.
- The temporary scaffold child self-expires within the readiness bound; no lifecycle command
  signals a PID or uses mutable PID authority.
- Stage B remains absent; no runner/BFF/API/storage/container/cloud surface exists.

## Related Code Files

Create exactly the 22 files listed under
[Commit 1](./stage-a-release-amendment.md#commit-1--exact-callable-scaffold-set). Create no other
product/test path. Released inputs remain read-only.

## Tests Before

No Stage A test file exists yet. Pre-scaffold release proofs are read-only input qualification,
not behavioral RED. Run only safe static inventory checks needed to establish the exact base and
confirm the scaffold commit has 22 creates and no target semantics.

## Implementation Steps

1. Prove exact base, remote equality, clean state, ownership non-overlap, and the exact
   correction-seed/validation-delta/readiness-delta derived input before any product write.
2. Recompute all 85 release rows, protected hashes, locks/tools, Make composition, and path
   absence.
3. Create the exact package/lock/config and callable scaffold modules/scripts/fragment.
4. Build and start the bounded neutral document through the real chain; let the scaffold child
   self-expire and classify scratch.
5. Scan scaffold bytes for forbidden released lesson/step/outcome/fixture strings, duplicate route
   truth, unconditional failure, mock/skip, runner/container/cloud imports, and source maps.
6. Commit once; record actual scaffold commit and tree. Prove the commit delta is exactly 22
   creates and tests remain absent.

## Success Criteria

- [ ] V3 branch and derived input satisfy the fresh recovery boundary.
- [ ] All 85 inputs and 33 absent paths are re-proved from exact integration.
- [ ] Exact 22-path callable scaffold commit exists with no target semantics or tests.
- [ ] Real build/server/lifecycle/evidence/Make/Chromium entry paths can be reached safely.
- [ ] Failed PR #29 and all v2 bytes remain untouched and unused.

## Risks and Rollback

| Risk | Mitigation |
|---|---|
| Scaffold accidentally implements target truth | Forbidden-string/AST/diff review and empty unadmitted registry invariant |
| Plan chain imports v2 product | Pre-scaffold diff restricted to plan directory; no v2 commit or artifact input |
| Temporary child becomes unsafe | Self-expiring bounded child; no PID signal |
| Released bytes drift | 85-row and seven-protected fail-closed verification |

Rollback removes only the 22 unmerged scaffold creates. It does not modify plan history, release
inputs, failed PR #29, or retained evidence.

## Next Steps

Phase 2 adds all eight tests in one tests-only commit and records contemporaneous RED before any
semantic implementation.
