# Issue #10 Stage A V3 Fresh Readiness Audit

## Verdict

`STAGE_A_READY` with two bounded plan-only corrections. Stage A cook scope is exactly
`stage-a-v3-static-portal`. Stage B remains blocked on Issue #9. This report is not product
implementation, implementation review, human approval, merge approval, release, or Issue
completion.

## Identity and Independence

- Audit input: `df013a3fd2cc87085eb1f7f264e6d25937a5ad13`.
- Correction authority: `a9e5ee26eacaec1b07ce9a25ac4b86da15f0b9a1`.
- Released integration: `5644f01b4c0443a81f3af0bcce80f44c847cd986`.
- Auditor: fresh Herdr Codex `gpt-5.6-sol`, reasoning effort `xhigh`; not the planner or validator.
- Initial local HEAD, configured upstream, and fresh live branch were equal to the audit input;
  the index/worktree was clean and Issue #10 had `ready for plan audit`.
- Failed PR #29 remained open and unchanged at rejected head
  `28a71ccc9028c61084a0aaed7fb1b426a62b6ba8`; no review or merge was synthesized.
- No active Issue #10 product writer was present. The only Issue #10 Herdr activity was this
  plan/readiness auditor; stopped v1/v2 worktrees were not inputs.

## Bounded Readiness Findings and Fixes

### RA-V3-01 — deterministic derived plan input

The plan required the v3 worktree to start directly from integration and then “apply the
correction output,” but did not name a reproducible seed because the correction commit modifies a
plan directory absent from integration. The fix binds the exact correction subtree
`92b620712d6fdbf69eb0901554dcf27ae357bc7e` (18 Markdown files), the exact validation delta
`a9e5ee2…df013a3`, and validation subtree
`60e9f41b517cf7ecfda9f85ce5038f8fadd8d981`; the published readiness delta and final subtree are
then verified from the Issue handoff before any product write. All pre-scaffold deltas must be
plan-directory-only. No v2 product, test, evidence, log, trace, manifest, fixture, generated byte,
or product commit may be copied or cherry-picked.

### RA-V3-02 — exact real-browser admission

“Same measured browser” alone could admit a non-Chrome-150 tuple. The fix pins the
readiness-observed Chrome `150.0.7871.181` executable SHA-256
`b724a4c5603cfc8b9d9f27a5153c8a39e7133e53666ced7f2a8b03bf49484f85` for both RED and GREEN,
while the absolute executable path remains local-private. Identity drift requires a new bounded
amendment and fresh readiness audit.

Both findings were plan-only, mechanically decidable, and did not change product scope, test
scope, commands, released inputs, product truth, security posture, or Stage B authority.

## Exact Scope Recomposition

- Stage A tracked paths: 33/33 unique create-only paths; 0 modifies; 0 deletes; no third class.
- Integration absence: 33/33 absent from the released tree.
- Callable semantics-free scaffold: 22/22 unique paths.
- Complete tests-only commit: 8/8 unique paths.
- Later creates: 3/3 unique paths.
- Decomposition: exact disjoint union `33 = 22 + 8 + 3`.
- Command shapes: 18/18 unique.
- Released read-only catalogue: 85/85 unique paths; every Git blob, byte count, and SHA-256
  recomputed from integration and exact.
- Protected identities: 7/7 exact.
- Integration tree: 921 entries, tree `a38594d420fe7df2b30265a8a72bb5fad1698012`, canonical listing
  SHA-256 `a6681b3e7ee932fbd29728bc3f649017e57e6980871a3de9def9cb3ac318d9fe`.
- Root Make: byte-exact sorted `mk/issue-5/*.mk` include seam; 9/9 I5-05 reservations are
  `future-owner` / `not-runnable`; direct fragment variants remain denied.

Fresh intersections were zero against active Issue #9 runner branches, Issue #11 feature/PR
paths, and Issue #12/#13 planned write catalogues. Issue #10 writes only its exact 33 paths.

## Scaffold-First TDD Cookability

Commit 1 is an exact callable 22-path scaffold with empty unadmitted descriptor state and no
promotion lesson/step/outcome/fixture/default-route semantics. Commit 2 adds only the eight
complete test paths. At that tree all real adapter/provider/catalog/router/render/build/server,
lifecycle, evidence, public Make, and Chrome 150 paths execute against the scaffold.

All behavior-bearing commands must fail for a named absent semantic after real reachability; setup
controls may pass. Missing tool/path/import, forced failure, expected echo, mock, skip,
predicate-only/static reimplementation, fixture fallback, and retrospective RED are rejected.
The catalogue contains 14/14 unique RED IDs and ten unique named classifications. Raw and
sanitized RED logs bind the actual scaffold/tests commits and trees before the first semantic
commit; GREEN binds the final head/tree and unchanged test blobs.

## Singular Production Authority

The sole production root is released
`learning/contracts/learning-contract-set-v1.json` at SHA-256
`92aaf9a573f5d23b5bf5d8d7db1e68150d4b0944f0e6ab6e651b1a3d34408638`, plus its exact hash-bound
manifest/lesson/lab members and shared Vite binding. One immutable
`ReleasedPortalDescriptorRegistry` drives catalog, app, router, static routes/documents, and React.
Production has no duplicate `defaultCatalog`, `STEP_IDS`, route table, promotion switch, copied
fixture/schema, or alias map.

Branded test-only structural descriptors exist only in unit-test memory, exercise pure functions,
are rejected by production, and must be absent from build/runtime/evidence inventories. Future
Issue #11/#12 content requires a reviewed released-registry amendment and later exact-SHA
validation/readiness; no content-ID branch is authorized.

## Evidence, Trace, and Defensive Closure

One fresh current generation per tested head contains hash-valid raw/sanitized RED and GREEN logs
for all 18 command rows, current resource/S3/lifecycle/blocked-result/interruption/cleanup/rollback
records, and exactly one successful real Chrome-channel Chromium journey trace. Trace mode is on
only for the journey; sources are disabled; compressed/uncompressed size, entry count, safe paths,
privacy, type/mode/owner/link, and SHA-256 are verified.

The non-self `inventory.json` → `generation-index.json` → atomically renamed
`current-generation.json` chain closes count, size, aggregate, privacy, source/tree/input/tool,
and artifact identity. Pending/failed/stale generations cannot become current and are retained
only as verified negative history. Cleanup is owner/marker/nonce-bound, runs twice, preserves
selected/negative-history evidence, and classifies every ignored or generated node.

The role record truthfully labels current evidence as author/cook-generated with independent
review and human approval false. Later independent review records bind the exact final head/tree
and generation-index digest without rewriting author evidence.

All seven defensive requirements remain mandatory: child-authenticated self-shutdown without PID
authority; schema-valid blocked `fitness-result-v2`; closed build/request policy; exact
runtime/lock/environment admission; generic registry seam; bounded artifacts; and full RED/S3
coverage. PTP-S3-01..14 are unique and require real valid-control/negative proof with no skip.

## Product and Resource Contract

Released truth is ten ordered narrative steps, four independent grains with 7/25/47/10 sanitized
rows, and only `insufficient-evidence/no-common-grain`. The Stage A portal is Vietnamese-first,
desktop/narrow, keyboard/focus/overflow/reduced-motion checked, axe Critical/Serious zero, and
equivalent without JavaScript. Runner, execution, reset, fresh evidence, progress, completion,
course maturity, and full-product claims remain unavailable or prohibited.

Readiness observed exact Node `22.22.3`, npm `10.9.8`, CPython `3.12.3`, 16 GiB RAM, more than
24 GiB free disk, and the pinned Chrome 150 executable. The released lock admits Playwright
`1.61.1`; the plan preserves explicit time/process/file/count/byte ceilings. No package install,
build, portal test, browser journey, runner, container, cloud, AWS, or Terraform action was run by
this audit.

## Machine and Whole-Plan Checks

- CK 4.5.2 strict JSON: valid, seven phases, zero issues.
- CK status JSON: seven pending, zero complete/in-progress, 0% implementation progress.
- Markdown: 20 artifacts, 64/64 local links/anchors.
- Placeholder/current-authority scan: no unresolved value.
- Scope/command/RED/S3/evidence/trace/protected/private-path/secret/diff checks: pass.
- Current Git diff: Issue #10 plan/readiness Markdown only; no product/test/evidence path.
- Whole-plan sweep: branch replay, browser identity, scaffold chronology, generic authority,
  evidence/trace, defensive gates, Vietnamese static contract, Stage B block, and no-full-product
  claim are consistent after the bounded corrections.

## Stage B and Disposition

Issue #9 remains open with a plan/cook candidate but no published feature branch, reviewed PR,
merge, or pristine release handoff. Stage B file, command, dependency, transport, execution,
evidence, reset, progress, and completion authorities remain `[]`. Stage B is
`blocked-on-issue9`.

After this report and bounded fixes are committed and pushed with local/upstream/live equality,
Issue #10 may move from `ready for plan audit` to `ready to cook` for Stage A v3 only. Failed PR
#29 and all v2 evidence remain immutable. No future approval is synthesized.
