---
title: "Issue #10 Stage A v3 post-review plan correction report"
reportDate: "2026-07-23"
inputSha: "2f278eb25aaff9e050314b01d1be155b76793f11"
integrationReleaseSha: "5644f01b4c0443a81f3af0bcce80f44c847cd986"
verdict: READY_FOR_INDEPENDENT_VALIDATION
reviewFindings: "3/3 addressed at plan level"
stageB: blocked-on-issue9
cloudAction: none
---

# Issue #10 Stage A V3 Post-Review Plan Correction Report

## Summary

This report records the plan-only correction after failed PR #29 review. The correction preserves
the released product boundary and fixes the three root causes: scaffold/RED chronology, generic
content authority, and retained-evidence closure. It does not validate readiness, authorize cook,
modify failed implementation history, or perform product/runner/container/cloud work.

## Authority

- Failed review: https://github.com/khanhvg/ai-ready-data-platform/pull/29#issuecomment-5050218543
- Fresh recovery authorization:
  https://github.com/khanhvg/ai-ready-data-platform/issues/10#issuecomment-5050239390
- Exact correction input: `2f278eb25aaff9e050314b01d1be155b76793f11`.
- Exact future recovery base: `5644f01b4c0443a81f3af0bcce80f44c847cd986`, tree
  `a38594d420fe7df2b30265a8a72bb5fad1698012`.
- Failed PR #29 and `feature/issue-10-portal-stage-a-v2` remain immutable negative history.

## Findings Corrected

| Finding | Correction | Plan proof |
|---|---|---|
| Incomplete core/browser RED provenance | Exact 22-path callable semantics-free scaffold commit, exact eight-path tests-only commit, contemporaneous all-command/real-Chromium RED, then first/later/final semantic commits with actual commit/tree/log bindings | Amendment chronology; Phases 1–4; verification RED matrix |
| Duplicate/invented generic authority | `learning-contract-set-v1.json` is the sole production descriptor-registry root; current exact Issue #8 hashes only; catalog/app/router/static/React derive from one registry; branded test-only structures are pure-function-only, production-rejected, and build/runtime-absent | Architecture registry and test-only sections; Phase 3 |
| Invalid retained hashes and absent trace | One atomic current generation per tested head; every current entry verifies through non-self closure; stale/prior/failed generations are negative history; exactly one successful real Chromium trace with sources false and exact archive/privacy/size/hash inventory | Amendment current-generation contract; Phase 4; verification trace/evidence sections |

`REVIEW_FINDINGS=3/3` at plan-correction level.

## Exact Scope Revalidation

The normative release amendment parses to:

- 33 unique create-only product/test paths;
- 18 unique command shapes;
- 85 unique released read-only inputs.

Every 85-row path/blob/byte/SHA-256 identity was recomputed from the exact released Git object.
Every one of the 33 product/test paths is absent from the v3 integration base. Root Make contains
the sorted `mk/issue-5/*.mk` include seam; the released command-owner registry contains nine exact
I5-05 reservations for `mk/issue-5/i5-05.mk`. No root edit is authorized.

## Scaffold-First Chronology

| Role | Exact planned delta | Semantic state |
|---|---:|---|
| Recovery input | Integration plus audited plan/validation/readiness commits only | No product/test/evidence |
| Scaffold commit | 22 creates | Callable neutral chain; no target lesson/routes/outcome/fixture/default truth |
| Tests commit | Eight creates, no scaffold change | Complete unit/defensive/Make/browser/visual tests |
| RED generation | No tracked change | Raw/sanitized logs from tests-only head; all real entrypoints bound |
| First semantic commit | Existing scaffold paths only | Exact released registry/hash admission |
| Portal semantics | Three remaining creates plus scaffold modifications | Registry-derived Vietnamese-first current slice |
| Defensive/final semantics | Existing 33-path set only | Seven defensive requirements and final activation/evidence |

Tests cannot fail for missing tool/import/path, forced failure, expected echo, mock/skip,
predicate-only oracle, or fixture fallback. A test change after RED requires a new tests-only
commit and fresh contemporaneous RED before matching behavior.

## Generic Authority Closure

Current production admits only the exact released contract-set, promotion manifest/lesson/lab,
shared Vite binding, and their catalogue hashes. One immutable
`ReleasedPortalDescriptorRegistry` drives catalog, app, router, static routes, static documents,
and React output. Production has no duplicate default catalog, step array, route table, promotion
switch, or local alias map.

Metamorphic tests use the exact released descriptor and separately branded
`test-only-structure` values in pure functions. Production admission rejects the brand with
`PORTAL_DESCRIPTOR_AUTHORITY_FORBIDDEN`; bundle/build/runtime inventories prove its tokens absent.
Those tests prove generic structure, not a second or future release.

Future #11/#12 content can enter only after a reviewed integration adds exact hash-bound entries
to the released registry/binding and a later Issue #10 amendment pins them. No current code-level
content ID is guessed.

## Evidence and Trace Closure

The plan requires one owner-private pending generation, non-self payload inventory, generation
index, and selector written last by atomic rename. Every current entry carries exact
owner/mode/type/link/media/privacy/size/hash plus source/tree/input/dependency/tool binding. Raw and
sanitized RED/GREEN command logs, resource and S3 records, lifecycle, blocked results,
interruption, cleanup, rollback, browser records, screenshots, and trace are current-generation
and hash-valid.

The real journey records exactly one Playwright trace with mode on and sources false. Archive
entry count, compressed/uncompressed size, safe paths, source absence, privacy, and SHA-256 are
verified. A missing or second trace fails. Stale/failed/prior generations are retained only as
negative history and can never satisfy current evidence. Interrupted pending bytes are never
selected.

## Seven Defensive Requirements

1. Child-authenticated private control with child self-shutdown; caller never signals mutable PID
   state and a tampered record cannot affect a foreign sentinel.
2. Both Stage B commands emit released-schema-valid blocked `fitness-result-v2`, exit 2, stderr
   only, with exact bindings and no runner/container/network action.
3. Closed regular-file build inventory and strict Host/GET/HEAD/zero-body/no-chunking request
   policy.
4. Exact runtime, lock, freeze, package graph, environment sanitization, and network admission.
5. One released registry generic seam with metamorphic tests and production negative gates.
6. Exact process/time/file/count/byte/type/mode/owner/link/privacy/trace/aggregate bounds.
7. Complete PTP-RED-A and PTP-S3-01..14 valid-control/mutation coverage through real paths.

## Product Truth

Stage A remains Vietnamese-first catalog → presentation-only module → promotion-trust lesson →
ten released steps, at desktop and 360px narrow layouts. It requires keyboard/focus/overflow,
reduced motion, axe Critical/Serious zero, no-JavaScript parity, and honest static maturity. The
four released grains remain independent; the only outcome is
`insufficient-evidence/no-common-grain`.

Runner, execution, reset, fresh learner evidence, progress, completion, and the full learning
product remain unavailable or disabled. Stage A is meaningful but incomplete. Stage B remains
blocked on Issue #9.

## Recovery and Non-Overlap

After later fresh validation and readiness only, v3 uses a new
`feature/issue-10-portal-stage-a-v3` worktree from exact integration. It applies only audited
copies of this v3 correction output plus its ensuing fresh independent validation and readiness
plan commits before the scaffold. No v2 product, tests, logs, trace, manifests, evidence,
fixtures, generated bytes, or v2 plan authority are copied or cherry-picked.

Issue #10 owns only its exact 33 paths. Issue #9 runner, Issue #11 curriculum, Issue #12 data labs,
Issue #13 profiles, shared contracts, root Make, README/docs, containers, cloud, AWS, and Terraform
remain outside scope. Stage B authority arrays remain empty.

## Local Publication Gates

The plan-only correction passed these author gates on 2026-07-23:

- CK 4.5.2 strict JSON validation: valid, zero issues, seven phase links; JSON status: seven
  pending, zero implementation progress.
- 18 Markdown files and 59 local links/anchors: zero broken targets; no unresolved authority
  substitution field. The lexical hits are prior scan-status statements or explicit
  forbidden-test/scaffold prose, not unresolved values.
- Exact catalogues: 33/33 unique final paths, 18/18 unique command shapes, 85/85 unique released
  inputs, 85/85 path/blob/byte/SHA-256 identities, and 33/33 base-path absences.
- Exact chronology: 22/22 scaffold paths, 8/8 test paths, 3/3 later create paths; 14/14 RED IDs
  map to ten named classifications and the real 18-command recorder.
- Generic authority, lifecycle, blocked result, build/request, runtime/lock/environment, bounded
  artifacts, evidence/interruption/cleanup, trace, and 14/14 S3 control/mutation contracts are
  mutually consistent. No active configuration disables tracing and no trace-absence oracle
  exists.
- Seven/seven protected identities match; root Make retains the sorted Issue #5 fragment seam;
  the released owner registry has nine/nine I5-05 reservations.
- Released truth parses to ten narrative steps and four independent sources with 89 sanitized
  rows; the lesson and fixture agree on `insufficient-evidence/no-common-grain`, and the released
  v2 fitness schema admits `status: fail` for the blocked Stage B result.
- Exact Issue #10 path intersection is zero with the current Issue #9 and #11 feature diffs and
  with the Issue #12/#13 plan path catalogues. Stage B authority remains empty.
- High-confidence secret material and literal private-path scans are empty. Cloud/runner/
  container/AWS/Terraform terms occur only in denied-action or Stage B blocked contracts; no such
  action was performed.
- The pre-commit diff is 16 Issue #10 plan/readiness/report artifacts, zero product/test paths,
  and zero whitespace errors. Fifteen edits are already tracked; the new report is deliberately
  ignored by the repository-wide `plans/**/*` rule and is the sole force-add classification.

Publication still requires the focused commit/push, post-push local/upstream/live equality, and
the exact label/comment handoff. The output commit SHA is recorded in Git and the Issue #10
publication comment because a commit cannot contain its own identity.

## Disposition

Plan-correction verdict: `READY_FOR_INDEPENDENT_VALIDATION`. This is not a readiness verdict,
review approval, or cook authority; cook scope remains none. The next legal phase after
successful publication is fresh independent plan validation, followed by a separate fresh
readiness audit.
