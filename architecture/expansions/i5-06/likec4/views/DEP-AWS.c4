view DEP-AWS {
  title "private-subnet; OpenMetadata compute database search; adapters_instance -> retail_instance"
  include author verify publish restore adapters_instance retail_instance
  deployment private-subnet
}
