import {createHash} from 'node:crypto'; import {readFileSync} from 'node:fs'; import {resolve} from 'node:path';
const paths={retail:'contracts/data/retail-golden-v1.json',contract:'contracts/data/promotion-trust-v1.yaml',evidence:'tests/fixtures/learning/promotion-trust/evidence-v1.json',manifest:'tests/fixtures/learning/promotion-trust/manifest.json'};
export function loadFixture(){const root=resolve(process.cwd(),'../../../..');const bytes=Object.fromEntries(Object.entries(paths).map(([k,p])=>[k,readFileSync(resolve(root,p))]));return{retail:JSON.parse(bytes.retail),contractText:bytes.contract.toString(),evidence:JSON.parse(bytes.evidence),manifest:JSON.parse(bytes.manifest),digests:Object.fromEntries(Object.entries(bytes).map(([k,b])=>[k,createHash('sha256').update(b).digest('hex')]))}}
