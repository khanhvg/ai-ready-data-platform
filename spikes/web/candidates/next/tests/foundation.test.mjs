import assert from 'node:assert/strict'; import {existsSync,readFileSync} from 'node:fs'; import {resolve} from 'node:path'; import test from 'node:test';
const root=resolve(import.meta.dirname,'../../../../..'), candidate=resolve(root,'spikes/web/candidates/next');
test('NEXT-FOUNDATION-001 pins standalone App Router and exact supply chain',()=>{const p=JSON.parse(readFileSync(resolve(candidate,'package.json')));assert.deepEqual(p.dependencies,{next:'16.2.10',react:'19.2.7','react-dom':'19.2.7'});assert.deepEqual(p.devDependencies,{'@axe-core/playwright':'4.12.1','@playwright/test':'1.61.1'});assert.match(readFileSync(resolve(candidate,'next.config.mjs'),'utf8'),/output:\s*['"]standalone/)});
test('NEXT-FIXTURE-001 directly reads all four tracked inputs',()=>{const s=readFileSync(resolve(candidate,'lib/fixture.mjs'),'utf8');for(const p of ['contracts/data/retail-golden-v1.json','contracts/data/promotion-trust-v1.yaml','tests/fixtures/learning/promotion-trust/evidence-v1.json','tests/fixtures/learning/promotion-trust/manifest.json'])assert.ok(s.includes(p));assert.doesNotMatch(s,/synthetic-promotion|5282cc6698c34e5d/)});
test('NEXT-SEMANTIC-001 prerenders ten acts and four grains with bounded conclusion',()=>{const html=readFileSync(resolve(candidate,'.next/server/app/index.html'),'utf8');assert.equal((html.match(/data-act=/g)||[]).length,10);for(const g of ['promo_name, channel','carrier, region_name','reason, category_name, region_name','scenario'])assert.ok(html.includes(g));assert.ok(html.includes('insufficient-evidence'));assert.ok(html.includes('no-common-grain'));assert.doesNotMatch(html,/completed|promotion_caused|attributed_to/i);assert.ok(existsSync(resolve(candidate,'.next/standalone/server.js')))});
