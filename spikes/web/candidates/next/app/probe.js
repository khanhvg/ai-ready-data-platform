'use client'; import {useState} from 'react'; export default function Probe(){const[s,setS]=useState('Exploration is reversible and unverified.');return <div><button onClick={()=>setS('Controlled failure: no common grain; no answer or score saved.')}>Run bounded probe</button><button onClick={()=>setS('Reset: baseline restored; no answer or score persisted.')}>Reset</button><p role="status" aria-live="polite">{s}</p></div>}
