import {loadFixture} from '../../../lib/fixture.mjs'; export const dynamic='force-static'; export function GET(){const{evidence,manifest,digests}=loadFixture();return Response.json({schemaVersion:'i5-02-read-only-fixture-view-v1',manifestSchemaVersion:manifest.schemaVersion,decision:evidence.decision,grains:evidence.sources.map(s=>s.grain),digests},{headers:{'Cache-Control':'no-store'}})}
