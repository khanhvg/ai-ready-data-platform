import React,{useState}from'react';import{createRoot}from'react-dom/client';import'./styles.css';function Probe(){const[s,setS]=useState('Exploration is reversible and unverified.');return <><button onClick={()=>setS('Controlled failure: no common grain; no answer or score saved.')}>Run bounded probe</button><button onClick={()=>setS('Reset: baseline restored; no answer or score persisted.')}>Reset</button><p role="status" aria-live="polite">{s}</p></>}const root=document.getElementById('probe');if(root)createRoot(root).render(<Probe/>);
